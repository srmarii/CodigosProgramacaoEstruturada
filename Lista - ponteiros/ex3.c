/* Crie um programa que monta o menu (cadastrar, listar, pesquisar e
sair) para cadastrar 10 números inteiros. Esse programa possui as
seguintes funções:
menu() – que imprime o menu e lê a opção selecionada pelo usuário.
cadastrar() - que cadastra (lê) todos os elementos de um vetor.
listarTodos() – que lista todos os elementos do vetor.
pesquisar() – que recebe o vetor e um elemento que será pesquisado
dentro do vetor (parâmetro por referência ).*/

#include <stdio.h>
#include <string.h>


int main(){
    return 0;
}